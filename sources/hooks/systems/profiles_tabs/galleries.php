<?php /*

 The contents of this file are subject to the Common Public Attribution License Version 1.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at http://opensource.org/licenses/cpal_1.0.

 Software distributed under the License is distributed on an "AS IS" basis, WITHOUT WARRANTY OF ANY KIND, either express or implied.
 See the License for the specific language governing rights and limitations under the License.

 The Original Code is Composr CMS.

 The Original Developer is the Initial Developer.

 The Initial Developer of the Original Code is Chris Graham.
 All portions of the code written by Chris Graham are Copyright (c) Christopher Graham. All Rights Reserved.

 See docs/LICENSE.md for full licensing information.

*/

/*
 NOTE TO PROGRAMMERS:
   Do not edit this file. If you need to make changes, save your changed file to the appropriate *_custom folder
   **** If you ignore this advice, then your website upgrades (e.g. for bug fixes) will likely kill your changes ****
*/

/**
 * @license    http://opensource.org/licenses/cpal_1.0 Common Public Attribution License
 * @copyright  Christopher Graham
 * @package    galleries
 */

/**
 * Hook class.
 */
class Hook_profiles_tabs_galleries
{
    /**
     * Find whether this hook is active.
     *
     * @param  MEMBER $member_id_of The ID of the member who is being viewed
     * @param  MEMBER $member_id_viewing The ID of the member who is doing the viewing
     * @return boolean Whether this hook is active
     */
    public function is_active(int $member_id_of, int $member_id_viewing) : bool
    {
        if (!addon_installed('galleries')) {
            return false;
        }

        if (!has_privilege($member_id_of, 'have_personal_category', 'cms_galleries')) {
            return false;
        }
        if ($member_id_of == $member_id_viewing) {
            if ($GLOBALS['SITE_DB']->query_select_value_if_there('galleries', 'is_member_synched', ['is_member_synched' => 1]) !== null) {
                return true; // Can have one, even if don't know
            }
        }
        return ($this->find_num_personal_galleries($member_id_of) > 0); // Does have one
    }

    /**
     * Find number of personal galleries of a member.
     *
     * @param  MEMBER $member_id_of The ID of the member
     * @return integer Number of personal galleries
     */
    protected function find_num_personal_galleries(int $member_id_of) : int
    {
        static $result = [];
        if (!isset($result[$member_id_of])) {
            $sql = 'SELECT COUNT(*) FROM ' . get_table_prefix() . 'galleries WHERE name LIKE \'' . db_encode_like('member\_' . strval($member_id_of) . '\_%') . '\'';
            $result[$member_id_of] = $GLOBALS['SITE_DB']->query_value_if_there($sql);
        }
        return $result[$member_id_of];
    }

    /**
     * Render function for profile tab hooks.
     *
     * @param  MEMBER $member_id_of The ID of the member who is being viewed
     * @param  MEMBER $member_id_viewing The ID of the member who is doing the viewing
     * @param  boolean $leave_to_ajax_if_possible Whether to leave the tab contents null, if this hook supports it, so that AJAX can load it later
     * @return array A tuple: The tab title, the tab contents, the suggested tab order, the icon
     */
    public function render_tab(int $member_id_of, int $member_id_viewing, bool $leave_to_ajax_if_possible = false) : array
    {
        require_lang('galleries');

        $title = ($this->find_num_personal_galleries($member_id_of) <= 1) ? do_lang_tempcode('GALLERY') : do_lang_tempcode('GALLERIES');

        $order = 30;

        if ($leave_to_ajax_if_possible) {
            return [$title, null, $order, 'menu/rich_content/galleries'];
        }

        $galleries = do_block('main_personal_galleries_list', ['member_id' => strval($member_id_of)]);

        // Render
        $content = do_template('CNS_MEMBER_PROFILE_GALLERIES', [
            '_GUID' => 'ede1039bfd9bacfabb1d38e9e6821465',
            'MEMBER_ID' => strval($member_id_of),
            'GALLERIES' => $galleries,
        ]);

        return [$title, $content, $order, 'menu/rich_content/galleries'];
    }
}
